<div class="row">
  <div class="large-12 columns">
    <h1>{{venue}}<span class="subheader"> - {{format-date startsAt 'ddd, ll'}}</span></h1>
    <h2>Where is it happening?</h2>
  </div>
</div>
<div class='row'>
  <div class='large-8 columns'>
    {{map-canvas dinner=this}}
  </div>
  <div class='large-4 columns'>
    <div class="panel venue-details">
      <dl>
        <dt>Venue:</dt>
        <dd>{{venue}}</dd>
        <dt>Date:</dt>
        <dd>{{format-date startsAt 'll'}}</dd>
        <dt>Starts at:</dt>
        <dd>{{format-date startsAt 'h:mm a'}}</dd>
        <dt>Address:</dt>
        <dd>{{address}}</dd>
      </dl>
    </div>
  </div>
</div>
<div class="row">
  <div class="large-12 columns">
    <hr/>
    <h2>Who's eating what?</h2>
    <nav class='breadcrumbs'>
      {{#link-to 'starters' activeClass='current'}}Starters{{/link-to}}
      {{#link-to 'main' activeClass='current'}}Main{{/link-to}}
      {{#link-to 'dessert' activeClass='current'}}Dessert{{/link-to}}
    </nav>
    </br>
    {{outlet}}
  </div>
</div>
